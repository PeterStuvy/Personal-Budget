from budget import budget
from datetime import datetime
from click import click

#Create instance for the budget
my_budget = budget()

options = ['Add an account', 'Add a transaction', 'View transactions or accounts', 'Set a budget limit', 'View budget limits', 'View the remaining budget for the month']
